# nacos配置 存在namespace 和group概念
server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
#        group: DEV_GROUP
        group: TEST_GROUP
        namespace: cbf4f738-3415-4ba4-ac86-dcb9216ad335
